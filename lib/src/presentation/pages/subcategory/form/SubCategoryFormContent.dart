import 'package:flutter/widgets.dart';

class SubCategoryFormContent extends StatelessWidget {
  const SubCategoryFormContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
